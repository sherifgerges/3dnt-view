"""DEPRECATED — no longer used.

The app previously offered a second engine (the structure-informed-rvas
permutation scan) that required a SIR_REPO clone. That path was removed; the
app now uses only the self-contained Fisher neighborhood test in
``fisher_scan.py``. This file is kept as a placeholder and can be deleted.
"""
